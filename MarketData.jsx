import {useEffect,useState} from "react";import {WATCH_SYMBOLS} from "../data/mockData";import {Sparkline,Spinner} from "../components/UI";
export default function MarketData({finnhubKey,setFinnhubKey}){const [temp,setTemp]=useState(finnhubKey||"");const [quotes,setQuotes]=useState({});const [loading,setLoading]=useState(false);async function refresh(){if(!finnhubKey)return;setLoading(true);const next={};for(const {sym} of WATCH_SYMBOLS){try{const r=await fetch(`https://finnhub.io/api/v1/quote?symbol=${sym}&token=${finnhubKey}`);const d=await r.json();next[sym]={c:d.c,dp:d.dp,h:d.h,l:d.l,pc:d.pc}}catch{}}setQuotes(next);setLoading(false)}useEffect(()=>{refresh()},[finnhubKey]);return <div className="fade-in"><div className="page-header"><div><div className="page-title">MARKT <span>DATEN</span></div><div className="page-sub">Live via Finnhub · PE Benchmarks</div></div><button className="btn btn-ghost btn-sm" onClick={refresh}>{loading?<Spinner/>:"⟳ REFRESH"}</button></div>{!finnhubKey?<div className="api-key-banner"><b>Finnhub API-Key erforderlich</b><input className="input" value={temp} onChange={e=>setTemp(e.target.value)} placeholder="sk_xxxxx"/><button className="btn btn-primary btn-sm" onClick={()=>setFinnhubKey(temp.trim())}>SPEICHERN</button></div>:<div className="connected"><span className="badge badge-green">● API VERBUNDEN</span><button className="btn btn-ghost btn-sm" onClick={()=>setFinnhubKey("")}>KEY ÄNDERN</button></div>}<div className="market-grid">{WATCH_SYMBOLS.map(({sym,relevance})=>{const q=quotes[sym];const pos=!q||q.dp>=0;return <div className="market-card" key={sym}><div className="market-card-sym">{sym} · {relevance}</div><div className="market-card-name">{sym}</div>{q?<><div className="market-card-price" style={{color:pos?"var(--accent)":"var(--red)"}}>${q.c?.toFixed(2)}</div><div className={pos?"green":"red"}>{q.dp>=0?"+":""}{q.dp?.toFixed(2)}%</div><Sparkline data={[q.pc||0,q.l||0,q.c||0,q.h||0]}/></>:<p className="muted">Keine Daten</p>}</div>})}</div></div>}
