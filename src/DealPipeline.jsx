import {ScoreBar,Pill} from "../components/UI";
export default function DealPipeline({deals}){const stages=["Initial Contact","Screening","NDA","LOI","Due Diligence","Closed"];return <div className="fade-in"><div className="page-header"><div><div className="page-title">DEAL <span>PIPELINE</span></div><div className="page-sub">{deals.length} aktive Targets</div></div></div><div className="stage-row">{stages.map(s=><div className="stage-card" key={s}><span>{s.toUpperCase()}</span><b>{deals.filter(d=>d.status===s).length}</b></div>)}</div><div className="card"><table className="table"><thead><tr><th>Unternehmen</th><th>Sektor</th><th>Revenue</th><th>EBITDA</th><th>Marge</th><th>Multiple</th><th>Score</th><th>Priorität</th></tr></thead><tbody>{[...deals].sort((a,b)=>b.score-a.score).map(d=><tr key={d.id}><td className="bold">{d.name}</td><td>{d.sector}</td><td>€{d.revenue}M</td><td className="green">€{d.ebitda}M</td><td>{d.margin}%</td><td>{d.multiple}x</td><td><ScoreBar value={d.score}/></td><td><Pill type={d.priority==="High"?"green":d.priority==="Medium"?"orange":"gray"}>{d.priority}</Pill></td></tr>)}</tbody></table></div></div>}
