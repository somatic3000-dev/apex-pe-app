import { useCallback, useEffect, useMemo, useRef, useState } from 'react';

const WATCH_SYMBOLS = [
  { sym: 'SPY', name: 'S&P 500 ETF', relevance: 'Macro Benchmark' },
  { sym: 'BX', name: 'Blackstone', relevance: 'PE Peer' },
  { sym: 'KKR', name: 'KKR & Co.', relevance: 'PE Peer' },
  { sym: 'APO', name: 'Apollo Global', relevance: 'PE Peer' },
  { sym: 'CG', name: 'Carlyle Group', relevance: 'PE Peer' },
  { sym: 'HYG', name: 'High Yield Bond ETF', relevance: 'Debt Market' },
  { sym: 'IWM', name: 'Russell 2000 ETF', relevance: 'Small Cap' },
  { sym: 'LQD', name: 'Corp Bond ETF', relevance: 'Credit Market' },
  { sym: 'VIX', name: 'Volatility Index', relevance: 'Risk Gauge' },
];

const initialFund = { name: 'APEX CAPITAL FUND I', vintage: 2022, aum: 120, dryPowder: 34, deployed: 86, irr: 24.3, moic: 1.87, dpi: 0.42 };
const initialPortfolio = [
  { id: 1, name: 'TechOps GmbH', sector: 'Managed IT', status: 'Active', entry: 2022, ebitda: 4.2, revenue: 22, entryMultiple: 7.8, currentMultiple: 9.4, equity: 12, irr: 31.2, moic: 1.94, score: 82, stage: 'Hold', notes: 'Starke wiederkehrende Umsätze. EBITDA-Expansion läuft.', aiScore: 88 },
  { id: 2, name: 'MedServ AG', sector: 'Healthcare', status: 'Active', entry: 2022, ebitda: 6.8, revenue: 38, entryMultiple: 9.2, currentMultiple: 11.1, equity: 18, irr: 22.4, moic: 1.72, score: 74, stage: 'Scale', notes: 'Roll-up von 3 Kliniken. Synergien realisiert.', aiScore: 61 },
  { id: 3, name: 'LogiStar GmbH', sector: 'Logistik', status: 'Active', entry: 2023, ebitda: 2.9, revenue: 18, entryMultiple: 6.5, currentMultiple: 7.8, equity: 8, irr: 18.7, moic: 1.43, score: 61, stage: 'Optimize', notes: 'Margen unter Druck. Kostenreduktion eingeleitet.', aiScore: 74 },
  { id: 4, name: 'Dental Gruppe', sector: 'Dental', status: 'Active', entry: 2023, ebitda: 3.4, revenue: 14, entryMultiple: 8.1, currentMultiple: 10.2, equity: 10, irr: 28.9, moic: 1.88, score: 78, stage: 'Roll-up', notes: '4 Praxen akquiriert. Ziel: 12 bis Exit.', aiScore: 45 },
  { id: 5, name: 'CloudSec Ltd.', sector: 'Cybersecurity', status: 'Active', entry: 2024, ebitda: 1.8, revenue: 9, entryMultiple: 11.2, currentMultiple: 13.5, equity: 8, irr: 41.2, moic: 1.32, score: 89, stage: 'Scale', notes: 'ARR-Wachstum 78% YoY. Marktführer in Nische.', aiScore: 95 },
  { id: 6, name: 'HVAC Masters', sector: 'Gebäude', status: 'Exit', entry: 2021, ebitda: 5.1, revenue: 26, entryMultiple: 6.0, currentMultiple: 8.8, equity: 14, irr: 33.6, moic: 2.41, score: 91, stage: 'Sold', notes: 'Verkauft an Stratege. 2.41x MOIC realisiert.', aiScore: 52 },
];
const initialDeals = [
  { id: 1, name: 'Phamex Pharma Services', sector: 'Healthcare', revenue: 31, ebitda: 5.8, margin: 18.7, multiple: 9.5, status: 'LOI', score: 86, priority: 'High' },
  { id: 2, name: 'DataCenter Nord', sector: 'Infrastruktur', revenue: 48, ebitda: 14.2, margin: 29.6, multiple: 12.1, status: 'Screening', score: 79, priority: 'High' },
  { id: 3, name: 'Compliance360', sector: 'Compliance SaaS', revenue: 12, ebitda: 3.1, margin: 25.8, multiple: 10.8, status: 'NDA', score: 72, priority: 'Medium' },
  { id: 4, name: 'AutoRepair Gruppe', sector: 'Auto-Services', revenue: 22, ebitda: 3.8, margin: 17.3, multiple: 7.2, status: 'Screening', score: 58, priority: 'Low' },
  { id: 5, name: 'EduTech Solutions', sector: 'Education SaaS', revenue: 8, ebitda: 1.9, margin: 23.8, multiple: 13.4, status: 'Initial Contact', score: 68, priority: 'Medium' },
];

function GlobalStyle() {
  return <style>{`
@import url('https://fonts.googleapis.com/css2?family=Bebas+Neue&family=DM+Mono:wght@300;400;500&family=DM+Sans:wght@300;400;500;600&display=swap');
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}:root{--bg:#0a0b0e;--surface:#111318;--surface2:#181c24;--border:#1f2535;--accent:#c8f542;--accent2:#42f5c8;--red:#f54242;--orange:#f5a742;--text:#e8eaf0;--muted:#5a6175;--font-head:'Bebas Neue',sans-serif;--font-mono:'DM Mono',monospace;--font-body:'DM Sans',sans-serif}body{background:var(--bg);color:var(--text);font-family:var(--font-body);font-size:14px;line-height:1.5;overflow-x:hidden}.app{display:grid;grid-template-columns:220px 1fr;grid-template-rows:56px 1fr;min-height:100vh}.topbar{grid-column:1/-1;background:var(--surface);border-bottom:1px solid var(--border);display:flex;align-items:center;padding:0 24px;gap:16px;position:sticky;top:0;z-index:100}.topbar-logo{font-family:var(--font-head);font-size:22px;letter-spacing:2px;color:var(--accent);flex:1}.topbar-logo span{color:var(--muted);font-size:13px;font-family:var(--font-mono);letter-spacing:0;margin-left:8px}.badge{font-family:var(--font-mono);font-size:11px;padding:3px 10px;border-radius:3px;border:1px solid}.badge-green{border-color:var(--accent);color:var(--accent);background:rgba(200,245,66,.08)}.badge-blue{border-color:var(--accent2);color:var(--accent2);background:rgba(66,245,200,.08)}.badge-red{border-color:var(--red);color:var(--red);background:rgba(245,66,66,.08)}.sidebar{background:var(--surface);border-right:1px solid var(--border);padding:20px 0;display:flex;flex-direction:column;gap:4px;position:sticky;top:56px;height:calc(100vh - 56px);overflow-y:auto}.nav-section{padding:16px 16px 4px;font-family:var(--font-mono);font-size:10px;color:var(--muted);letter-spacing:2px;text-transform:uppercase}.nav-item{display:flex;align-items:center;gap:10px;padding:9px 16px;cursor:pointer;color:var(--muted);font-size:13px;font-weight:500;border-left:2px solid transparent;transition:all .15s;user-select:none}.nav-item:hover{color:var(--text);background:var(--surface2)}.nav-item.active{color:var(--accent);border-left-color:var(--accent);background:rgba(200,245,66,.05)}.nav-icon{font-size:15px;width:18px;text-align:center}.main{padding:28px;overflow-y:auto}.page-header{display:flex;align-items:flex-end;justify-content:space-between;margin-bottom:28px}.page-title{font-family:var(--font-head);font-size:36px;letter-spacing:2px;color:var(--text);line-height:1}.page-title span{color:var(--accent)}.page-sub{font-family:var(--font-mono);font-size:11px;color:var(--muted);margin-top:4px}.card{background:var(--surface);border:1px solid var(--border);border-radius:4px;padding:20px}.card-title{font-family:var(--font-mono);font-size:11px;letter-spacing:2px;text-transform:uppercase;color:var(--muted);margin-bottom:12px}.kpi-grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:20px}.kpi-card{background:var(--surface);border:1px solid var(--border);border-radius:4px;padding:18px 20px;position:relative;overflow:hidden}.kpi-card::before{content:'';position:absolute;top:0;left:0;right:0;height:2px}.green::before{background:var(--accent)}.blue::before{background:var(--accent2)}.red::before{background:var(--red)}.orange::before{background:var(--orange)}.kpi-label{font-family:var(--font-mono);font-size:10px;color:var(--muted);letter-spacing:1.5px;text-transform:uppercase;margin-bottom:8px}.kpi-value{font-family:var(--font-head);font-size:34px;color:var(--text);line-height:1}.kpi-delta{font-family:var(--font-mono);font-size:11px;margin-top:6px}.grid-2{display:grid;grid-template-columns:1fr 1fr;gap:16px;margin-bottom:16px}.grid-65-35{display:grid;grid-template-columns:1.8fr 1fr;gap:16px;margin-bottom:16px}.table{width:100%;border-collapse:collapse}.table th{font-family:var(--font-mono);font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted);text-align:left;padding:10px 12px;border-bottom:1px solid var(--border)}.table td{padding:11px 12px;border-bottom:1px solid rgba(31,37,53,.5);font-size:13px}.table tr:hover td{background:var(--surface2)}.mono{font-family:var(--font-mono);font-size:12px}.bold{font-weight:600}.input{background:var(--surface2);border:1px solid var(--border);color:var(--text);font-family:var(--font-body);font-size:13px;padding:8px 12px;border-radius:3px;width:100%;outline:none}.input:focus{border-color:var(--accent)}.btn{font-family:var(--font-mono);font-size:12px;letter-spacing:1px;padding:8px 18px;border:1px solid;border-radius:3px;cursor:pointer;transition:all .15s;background:transparent}.btn-primary{border-color:var(--accent);color:var(--accent)}.btn-primary:hover{background:var(--accent);color:#000}.btn-ghost{border-color:var(--border);color:var(--muted)}.btn-ghost:hover{border-color:var(--text);color:var(--text)}.btn-sm{padding:5px 12px;font-size:11px}.pill{display:inline-flex;align-items:center;gap:5px;font-family:var(--font-mono);font-size:11px;padding:3px 9px;border-radius:2px;font-weight:500}.pill-green{background:rgba(200,245,66,.12);color:var(--accent)}.pill-red{background:rgba(245,66,66,.12);color:var(--red)}.pill-blue{background:rgba(66,245,200,.12);color:var(--accent2)}.pill-orange{background:rgba(245,167,66,.12);color:var(--orange)}.pill-gray{background:var(--surface2);color:var(--muted)}.dot{width:5px;height:5px;border-radius:50%;background:currentColor}.score-bar{display:flex;align-items:center;gap:8px}.score-track,.progress-track{flex:1;height:4px;background:var(--border);border-radius:2px;overflow:hidden}.score-fill,.progress-fill{height:100%;border-radius:2px;transition:width .6s}.progress-row{display:flex;flex-direction:column;gap:6px;margin-bottom:12px}.progress-header{display:flex;justify-content:space-between}.progress-label{font-size:12px;color:var(--text)}.progress-val{font-family:var(--font-mono);font-size:11px;color:var(--muted)}.progress-track{height:6px}.sparkline{display:flex;align-items:flex-end;gap:3px;height:40px}.spark-bar{flex:1;border-radius:2px 2px 0 0;min-height:4px}.market-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:20px}.market-card{background:var(--surface);border:1px solid var(--border);border-radius:4px;padding:16px;cursor:pointer;transition:border-color .15s}.market-card:hover,.market-card.selected{border-color:var(--accent)}.market-card-sym{font-family:var(--font-mono);font-size:10px;color:var(--muted);letter-spacing:2px;margin-bottom:6px}.market-card-name{font-size:12px;margin-bottom:10px;font-weight:500}.market-card-price{font-family:var(--font-head);font-size:28px;line-height:1}.market-card-change{font-family:var(--font-mono);font-size:11px;margin-top:5px}.tabs{display:flex;border-bottom:1px solid var(--border);margin-bottom:20px}.tab{font-family:var(--font-mono);font-size:11px;letter-spacing:1px;text-transform:uppercase;padding:10px 18px;cursor:pointer;color:var(--muted);border-bottom:2px solid transparent;margin-bottom:-1px}.tab.active{color:var(--accent);border-bottom-color:var(--accent)}.modal-overlay{position:fixed;inset:0;background:rgba(0,0,0,.75);display:flex;align-items:center;justify-content:center;z-index:1000;backdrop-filter:blur(4px)}.modal{background:var(--surface);border:1px solid var(--border);border-radius:6px;width:560px;max-height:90vh;overflow-y:auto;padding:28px}.modal-title{font-family:var(--font-head);font-size:24px;letter-spacing:2px;margin-bottom:20px;color:var(--accent)}.modal-footer{display:flex;gap:10px;justify-content:flex-end;margin-top:20px}.form-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.form-group{margin-bottom:14px}.form-label{font-family:var(--font-mono);font-size:10px;letter-spacing:1.5px;text-transform:uppercase;color:var(--muted);margin-bottom:6px;display:block}.chat-wrap{display:flex;flex-direction:column;height:500px}.chat-messages{flex:1;overflow-y:auto;display:flex;flex-direction:column;gap:12px;padding:4px 0}.msg{max-width:88%;padding:10px 14px;border-radius:4px;font-size:13px;line-height:1.6;white-space:pre-wrap}.msg-ai{background:var(--surface2);border:1px solid var(--border);align-self:flex-start}.msg-user{background:rgba(200,245,66,.1);border:1px solid rgba(200,245,66,.25);align-self:flex-end;color:var(--accent)}.chat-input-row{display:flex;gap:10px;margin-top:14px}.chat-input{flex:1}.typing{display:flex;gap:4px}.typing span{width:6px;height:6px;border-radius:50%;background:var(--muted);animation:bounce .9s infinite}.typing span:nth-child(2){animation-delay:.15s}.typing span:nth-child(3){animation-delay:.3s}@keyframes bounce{0%,60%,100%{transform:translateY(0)}30%{transform:translateY(-5px)}}@keyframes spin{to{transform:rotate(360deg)}}.spin{animation:spin 1s linear infinite;display:inline-block}.fade-in{animation:fadeIn .35s ease forwards}@keyframes fadeIn{from{opacity:0;transform:translateY(8px)}to{opacity:1;transform:none}}@media(max-width:1000px){.app{grid-template-columns:1fr}.sidebar{position:static;height:auto}.kpi-grid,.grid-2,.grid-65-35,.market-grid{grid-template-columns:1fr}.topbar{overflow:auto}.main{padding:18px}}
`}</style>;
}

function Sparkline({ data = [], color = 'var(--accent)' }) {
  if (!data.length) return null;
  const max = Math.max(...data), min = Math.min(...data), range = max - min || 1;
  return <div className="sparkline">{data.map((v, i) => <div key={i} className="spark-bar" style={{ height: `${((v - min) / range) * 85 + 15}%`, background: color }} />)}</div>;
}
function ScoreBar({ value }) { const color = value >= 80 ? 'var(--accent)' : value >= 60 ? 'var(--orange)' : 'var(--red)'; return <div className="score-bar"><div className="score-track"><div className="score-fill" style={{ width: `${value}%`, background: color }} /></div><span className="mono" style={{ color, minWidth: 28 }}>{value}</span></div>; }
function ProgressBar({ label, value, max = 100, color = 'var(--accent)' }) { const pct = Math.min((value / max) * 100, 100); return <div className="progress-row"><div className="progress-header"><span className="progress-label">{label}</span><span className="progress-val">{value}{max === 100 ? '%' : 'x'}</span></div><div className="progress-track"><div className="progress-fill" style={{ width: `${pct}%`, background: color }} /></div></div>; }
function Pill({ type = 'gray', children }) { return <span className={`pill pill-${type}`}><span className="dot" />{children}</span>; }
function Spinner() { return <span className="spin">⟳</span>; }
function fmtChg(dp) { if (dp == null) return '—'; return `${dp >= 0 ? '+' : ''}${Number(dp).toFixed(2)}%`; }

function useFinnhub(apiKey) {
  const [quotes, setQuotes] = useState({});
  const [candles, setCandles] = useState({});
  const [news, setNews] = useState([]);
  const [loading, setLoading] = useState(false);
  const [lastUpdate, setLastUpdate] = useState(null);
  const [error, setError] = useState(null);
  const BASE = 'https://finnhub.io/api/v1';
  const fetchQuote = useCallback(async (sym) => { const r = await fetch(`${BASE}/quote?symbol=${sym}&token=${apiKey}`); if (!r.ok) throw new Error(`HTTP ${r.status}`); const d = await r.json(); return { c: d.c, pc: d.pc, dp: d.dp, h: d.h, l: d.l, o: d.o }; }, [apiKey]);
  const fetchCandles = useCallback(async (sym) => { const to = Math.floor(Date.now() / 1000); const from = to - 30 * 86400; const r = await fetch(`${BASE}/stock/candle?symbol=${sym}&resolution=D&from=${from}&to=${to}&token=${apiKey}`); const d = await r.json(); return d.s === 'ok' ? d.c : []; }, [apiKey]);
  const fetchNews = useCallback(async () => { const today = new Date().toISOString().slice(0, 10); const weekAgo = new Date(Date.now() - 7 * 86400000).toISOString().slice(0, 10); const r = await fetch(`${BASE}/news?category=merger&from=${weekAgo}&to=${today}&token=${apiKey}`); const d = await r.json(); return Array.isArray(d) ? d.slice(0, 12) : []; }, [apiKey]);
  const refresh = useCallback(async () => { if (!apiKey) return; setLoading(true); setError(null); try { const qr = await Promise.allSettled(WATCH_SYMBOLS.map(({ sym }) => fetchQuote(sym).then(q => ({ sym, q })))); const nq = {}; qr.forEach(r => { if (r.status === 'fulfilled') nq[r.value.sym] = r.value.q; }); setQuotes(nq); const cr = await Promise.allSettled(WATCH_SYMBOLS.slice(0, 6).map(({ sym }) => fetchCandles(sym).then(c => ({ sym, c })))); const nc = {}; cr.forEach(r => { if (r.status === 'fulfilled') nc[r.value.sym] = r.value.c; }); setCandles(nc); setNews(await fetchNews()); setLastUpdate(new Date()); } catch (e) { setError(e.message); } setLoading(false); }, [apiKey, fetchQuote, fetchCandles, fetchNews]);
  useEffect(() => { if (!apiKey) return; refresh(); const id = setInterval(refresh, 60000); return () => clearInterval(id); }, [apiKey, refresh]);
  return { quotes, candles, news, loading, lastUpdate, error, refresh };
}

function Dashboard({ fund, portfolio, quotes }) {
  const active = portfolio.filter(p => p.status === 'Active');
  const avgIrr = active.length ? (active.reduce((s, p) => s + p.irr, 0) / active.length).toFixed(1) : '0.0';
  const avgMoic = active.length ? (active.reduce((s, p) => s + p.moic, 0) / active.length).toFixed(2) : '0.00';
  const totalEquity = active.reduce((s, p) => s + p.equity, 0);
  return <div className="fade-in"><div className="page-header"><div><div className="page-title"><span>{fund.name}</span></div><div className="page-sub">Vintage {fund.vintage} · Fund Manager Dashboard</div></div><span className="badge badge-green">LIVE</span></div><div className="kpi-grid">{[
    ['AUM', `€${fund.aum}M`, '+12% YTD', 'green', [80, 88, 95, 100, 108, 115, 120]], ['Gross IRR', `${fund.irr}%`, '+2.1pp vs. Benchmark', 'green', [14, 17, 19, 21, 22, 24, 24.3]], ['MOIC Portfolio', `${avgMoic}x`, 'Ziel: 2.5x', 'blue', [1.1, 1.3, 1.5, 1.65, 1.75, 1.83, 1.87]], ['Dry Powder', `€${fund.dryPowder}M`, '2 Deals in Pipeline', 'orange', [120, 110, 95, 80, 65, 42, 34]]].map(([l, v, d, c, bars]) => <div key={l} className={`kpi-card ${c}`}><div className="kpi-label">{l}</div><div className="kpi-value">{v}</div><div className="kpi-delta" style={{ color: c === 'orange' ? 'var(--orange)' : c === 'blue' ? 'var(--accent2)' : 'var(--accent)' }}>{d}</div><div style={{ marginTop: 10 }}><Sparkline data={bars} color={c === 'orange' ? 'var(--orange)' : c === 'blue' ? 'var(--accent2)' : 'var(--accent)'} /></div></div>)}</div>{quotes.SPY && <MarketContext quotes={quotes} />}<div className="grid-65-35"><div className="card"><div className="card-title">Portfolio Companies — Übersicht</div><CompanyTable portfolio={portfolio} compact /></div><div style={{ display: 'flex', flexDirection: 'column', gap: 16 }}><div className="card"><div className="card-title">Kapitalallokation</div><ProgressBar label="Deployed" value={fund.deployed} max={fund.aum} /><ProgressBar label="Dry Powder" value={fund.dryPowder} max={fund.aum} color="var(--accent2)" /><ProgressBar label="Avg. IRR" value={parseFloat(avgIrr)} max={40} color="var(--orange)" /></div><div className="card"><div className="card-title">Sektor-Mix</div>{Object.entries(active.reduce((a, p) => ({ ...a, [p.sector]: (a[p.sector] || 0) + p.equity }), {})).map(([s, v]) => <ProgressBar key={s} label={s} value={Math.round((v / totalEquity) * 100)} color="var(--accent2)" />)}</div></div></div></div>;
}
function MarketContext({ quotes }) { return <div style={{ background: 'var(--surface2)', border: '1px solid var(--border)', borderRadius: 4, padding: '10px 16px', marginBottom: 16, display: 'flex', gap: 24, alignItems: 'center', flexWrap: 'wrap' }}><span className="mono" style={{ color: 'var(--muted)', letterSpacing: 1 }}>MARKT-KONTEXT</span>{['SPY', 'BX', 'KKR', 'HYG'].map(sym => quotes[sym] && <span key={sym} className="mono"><span style={{ color: 'var(--muted)' }}>{sym} </span>${quotes[sym].c?.toFixed(2)} <span style={{ color: quotes[sym].dp >= 0 ? 'var(--accent)' : 'var(--red)' }}>{fmtChg(quotes[sym].dp)}</span></span>)}</div>; }
function CompanyTable({ portfolio, onSelect, selected }) { return <table className="table"><thead><tr><th>Unternehmen</th><th>Sektor</th><th>IRR</th><th>MOIC</th><th>Score</th><th>Stage</th></tr></thead><tbody>{portfolio.map(p => <tr key={p.id} onClick={() => onSelect?.(p.id)} style={{ cursor: onSelect ? 'pointer' : 'default', background: selected === p.id ? 'var(--surface2)' : undefined }}><td className="bold">{p.name}</td><td style={{ color: 'var(--muted)' }}>{p.sector}</td><td className="mono" style={{ color: p.irr >= 25 ? 'var(--accent)' : 'var(--orange)' }}>{p.irr}%</td><td className="mono" style={{ color: 'var(--accent2)' }}>{p.moic}x</td><td style={{ minWidth: 100 }}><ScoreBar value={p.score} /></td><td><Pill type={p.stage === 'Sold' ? 'gray' : p.stage === 'Roll-up' ? 'blue' : p.stage === 'Scale' ? 'green' : 'orange'}>{p.stage}</Pill></td></tr>)}</tbody></table>; }

function MarktDaten({ finnhubKey, setFinnhubKey }) {
  const [tempKey, setTempKey] = useState(finnhubKey || ''); const [selectedSym, setSelectedSym] = useState('BX'); const [tab, setTab] = useState('overview'); const { quotes, candles, news, loading, lastUpdate, error, refresh } = useFinnhub(finnhubKey); const q = quotes[selectedSym]; const c = candles[selectedSym] || []; const info = WATCH_SYMBOLS.find(s => s.sym === selectedSym);
  return <div className="fade-in"><div className="page-header"><div><div className="page-title">MARKT <span>DATEN</span></div><div className="page-sub">Live via Finnhub · PE Benchmarks & Makro · Auto-Refresh 60s</div></div><div style={{ display: 'flex', gap: 10, alignItems: 'center' }}>{lastUpdate && <span className="mono" style={{ color: 'var(--muted)', fontSize: 10 }}>Zuletzt: {lastUpdate.toLocaleTimeString('de-DE')}</span>}<button className="btn btn-ghost btn-sm" onClick={refresh} disabled={loading || !finnhubKey}>{loading ? <Spinner /> : '⟳ REFRESH'}</button></div></div>{!finnhubKey ? <div className="card" style={{ borderColor: 'rgba(245,167,66,.3)', marginBottom: 20 }}><div className="card-title" style={{ color: 'var(--orange)' }}>Finnhub API-Key</div><div style={{ display: 'flex', gap: 12 }}><input className="input" placeholder="sk_xxxxxxxxxxxxxxxx" value={tempKey} onChange={e => setTempKey(e.target.value)} onKeyDown={e => e.key === 'Enter' && setFinnhubKey(tempKey.trim())} /><button className="btn btn-primary" onClick={() => setFinnhubKey(tempKey.trim())}>SPEICHERN</button></div></div> : <div style={{ display: 'flex', alignItems: 'center', gap: 10, marginBottom: 16 }}><span className="badge badge-green">● API VERBUNDEN</span>{error && <span className="badge badge-red">⚠ {error}</span>}<button className="btn btn-ghost btn-sm" onClick={() => { setFinnhubKey(''); setTempKey(''); }}>KEY ÄNDERN</button></div>}<div className="market-grid">{WATCH_SYMBOLS.map(({ sym, name, relevance }) => { const mq = quotes[sym]; const mc = candles[sym] || []; const pos = !mq || mq.dp >= 0; const color = pos ? 'var(--accent)' : 'var(--red)'; return <div key={sym} className={`market-card ${selectedSym === sym ? 'selected' : ''}`} onClick={() => setSelectedSym(sym)}><div className="market-card-sym">{sym} · {relevance}</div><div className="market-card-name">{name}</div>{mq ? <><div className="market-card-price" style={{ color }}>${mq.c?.toFixed(2)}</div><div className="market-card-change" style={{ color }}>{fmtChg(mq.dp)} <span style={{ color: 'var(--muted)' }}>H: ${mq.h?.toFixed(2)} · L: ${mq.l?.toFixed(2)}</span></div><Sparkline data={mc.slice(-20)} color={color} /></> : <div className="mono" style={{ color: 'var(--muted)', marginTop: 8 }}>{finnhubKey ? loading ? 'Lade...' : 'Keine Daten' : 'API-Key fehlt'}</div>}</div>; })}</div><div className="grid-65-35"><div className="card"><div className="tabs">{['overview', 'chart', 'news'].map(t => <div key={t} className={`tab ${tab === t ? 'active' : ''}`} onClick={() => setTab(t)}>{t === 'overview' ? 'ÜBERSICHT' : t === 'chart' ? 'KURSVERLAUF' : 'NACHRICHTEN'}</div>)}</div>{tab === 'overview' && <div><div style={{ fontFamily: 'var(--font-head)', fontSize: 28, color: 'var(--accent)', marginBottom: 16 }}>{info?.name} <span style={{ color: 'var(--muted)', fontSize: 16 }}>{selectedSym}</span></div>{q ? <div className="grid-2">{[['Aktuell', `$${q.c?.toFixed(2)}`], ['Vortag', `$${q.pc?.toFixed(2)}`], ['Veränderung', fmtChg(q.dp)], ['Tageshoch', `$${q.h?.toFixed(2)}`], ['Tagestief', `$${q.l?.toFixed(2)}`], ['Eröffnung', `$${q.o?.toFixed(2)}`]].map(([l, v]) => <div key={l} style={{ display: 'flex', justifyContent: 'space-between', padding: '9px 12px', background: 'var(--surface2)', borderRadius: 3 }}><span style={{ color: 'var(--muted)' }}>{l}</span><span className="mono">{v}</span></div>)}</div> : <Empty text="API-Key erforderlich oder keine Daten." />}</div>}{tab === 'chart' && <div><div style={{ fontFamily: 'var(--font-head)', fontSize: 18, marginBottom: 12 }}>{selectedSym} — 30-Tage Kursverlauf</div>{c.length ? <><div style={{ display: 'flex', alignItems: 'flex-end', gap: 2, height: 160 }}>{c.slice(-30).map((v, i, arr) => { const min = Math.min(...arr), max = Math.max(...arr), h = ((v - min) / (max - min || 1)) * 140 + 10, up = i === 0 || v >= arr[i - 1]; return <div key={i} style={{ flex: 1, height: h, background: up ? 'var(--accent)' : 'var(--red)', borderRadius: '2px 2px 0 0', opacity: .75 }} title={`$${v.toFixed(2)}`} />; })}</div></> : <Empty text="Keine Chartdaten verfügbar." />}</div>}{tab === 'news' && <div>{news.length ? news.map((n, i) => <div key={i} style={{ padding: '12px 0', borderBottom: '1px solid var(--border)' }}><div style={{ cursor: 'pointer' }} onClick={() => window.open(n.url, '_blank')}>{n.headline}</div><div className="mono" style={{ color: 'var(--muted)', fontSize: 10 }}>{n.source} · {new Date(n.datetime * 1000).toLocaleDateString('de-DE')}</div></div>) : <Empty text="Keine aktuellen Nachrichten." />}</div>}</div><div style={{ display: 'flex', flexDirection: 'column', gap: 14 }}><div className="card"><div className="card-title">PE Markt-Sentiment</div>{[['Buyout Activity',62],['Exit-Klima',55],['Debt Availability',70],['LP Commitment',68]].map(([l,v])=><ProgressBar key={l} label={l} value={v} color={v>=60?'var(--accent)':'var(--orange)'} />)}</div><div className="card"><div className="card-title">PE Peer Performance</div>{['BX','KKR','APO','CG'].map(sym=>quotes[sym]&&<div key={sym} style={{display:'flex',justifyContent:'space-between',padding:'8px 0',borderBottom:'1px solid var(--border)',cursor:'pointer'}} onClick={()=>setSelectedSym(sym)}><span>{sym}</span><span className="mono" style={{color:quotes[sym].dp>=0?'var(--accent)':'var(--red)'}}>{fmtChg(quotes[sym].dp)}</span></div>)}</div></div></div></div>;
}
function Empty({ text }) { return <div className="mono" style={{ color: 'var(--muted)', padding: '30px 0', textAlign: 'center' }}>{text}</div>; }

function Portfolio({ portfolio, setPortfolio }) { const [selected, setSelected] = useState(null); const [tab, setTab] = useState('overview'); const [showAdd, setShowAdd] = useState(false); const [newCo, setNewCo] = useState({ name: '', sector: '', revenue: '', ebitda: '', equity: '', entryMultiple: '', irr: '', moic: '' }); const sel = portfolio.find(p => p.id === selected); const add = () => { if (!newCo.name) return; setPortfolio(p => [...p, { id: Date.now(), ...newCo, revenue:+newCo.revenue||0, ebitda:+newCo.ebitda||0, equity:+newCo.equity||0, entryMultiple:+newCo.entryMultiple||0, currentMultiple:+newCo.entryMultiple||0, irr:+newCo.irr||0, moic:+newCo.moic||1, status:'Active', stage:'Optimize', entry:new Date().getFullYear(), score:65, aiScore:60, notes:'' }]); setShowAdd(false); }; return <div className="fade-in"><div className="page-header"><div><div className="page-title">PORTFOLIO <span>MANAGEMENT</span></div><div className="page-sub">{portfolio.filter(p=>p.status==='Active').length} aktive Beteiligungen · {portfolio.filter(p=>p.status==='Exit').length} Exits</div></div><button className="btn btn-primary" onClick={()=>setShowAdd(true)}>+ BETEILIGUNG HINZUFÜGEN</button></div><div className="grid-65-35"><div className="card"><div className="card-title">Portfolio Companies</div><CompanyTable portfolio={portfolio} selected={selected} onSelect={id=>setSelected(id===selected?null:id)} /></div><div className="card">{sel ? <><div style={{ fontFamily:'var(--font-head)', fontSize:20, color:'var(--accent)', marginBottom:14 }}>{sel.name}</div><div className="tabs">{['overview','financials','ai'].map(t=><div key={t} className={`tab ${tab===t?'active':''}`} onClick={()=>setTab(t)}>{t.toUpperCase()}</div>)}</div>{tab==='overview'&&<><Pill type={sel.stage==='Roll-up'?'blue':'green'}>{sel.stage}</Pill><div style={{color:'var(--muted)',fontSize:12,marginTop:14}}>{sel.notes}</div></>}{tab==='financials'&&[['Revenue',`€${sel.revenue}M`],['EBITDA',`€${sel.ebitda}M`],['EBITDA-Marge',`${((sel.ebitda/sel.revenue)*100).toFixed(1)}%`],['Equity inv.',`€${sel.equity}M`],['Entry Multiple',`${sel.entryMultiple}x`],['Current Multiple',`${sel.currentMultiple}x`],['IRR',`${sel.irr}%`],['MOIC',`${sel.moic}x`]].map(([l,v])=><div key={l} style={{display:'flex',justifyContent:'space-between',padding:'8px 0',borderBottom:'1px solid var(--border)'}}><span style={{color:'var(--muted)'}}>{l}</span><span className="mono">{v}</span></div>)}{tab==='ai'&&<><div className="card-title">KI-Automatisierungspotenzial</div><ScoreBar value={sel.aiScore}/><ProgressBar label="Prozessautomatisierung" value={sel.aiScore}/><ProgressBar label="Headcount-Optimierung" value={Math.max(30,sel.aiScore-15)} color="var(--accent2)"/><ProgressBar label="EBITDA-Impact" value={Math.max(20,sel.aiScore-25)} color="var(--orange)"/></>}</> : <Empty text="← Unternehmen auswählen für Details" />}</div></div>{showAdd&&<Modal title="+ NEUE BETEILIGUNG" onClose={()=>setShowAdd(false)} onSave={add}>{[['Unternehmensname *','name'],['Sektor','sector'],['Umsatz (€M)','revenue'],['EBITDA (€M)','ebitda'],['Equity inv. (€M)','equity'],['Entry Multiple','entryMultiple'],['IRR (%)','irr'],['MOIC','moic']].map(([l,k])=><Field key={k} label={l} value={newCo[k]} onChange={v=>setNewCo({...newCo,[k]:v})}/>)}</Modal>}</div>; }
function Modal({ title, children, onClose, onSave }) { return <div className="modal-overlay" onClick={e=>e.target===e.currentTarget&&onClose()}><div className="modal"><div className="modal-title">{title}</div><div className="form-grid">{children}</div><div className="modal-footer"><button className="btn btn-ghost" onClick={onClose}>ABBRECHEN</button><button className="btn btn-primary" onClick={onSave}>SPEICHERN</button></div></div></div>; }
function Field({ label, value, onChange, type='text' }) { return <div className="form-group"><label className="form-label">{label}</label><input className="input" type={type} value={value} onChange={e=>onChange(e.target.value)} /></div>; }

function DealPipeline({ deals, setDeals }) { const stages = ['Initial Contact','Screening','NDA','LOI','Due Diligence','Closed']; const [showAdd,setShowAdd]=useState(false); const [d,setD]=useState({name:'',sector:'',revenue:'',ebitda:'',multiple:'',status:'Screening',priority:'Medium'}); const add=()=>{ if(!d.name)return; const rev=+d.revenue||0, ebi=+d.ebitda||0; setDeals(x=>[...x,{id:Date.now(),...d,revenue:rev,ebitda:ebi,multiple:+d.multiple||0,margin:rev?((ebi/rev)*100).toFixed(1):0,score:Math.floor(Math.random()*30)+55}]); setShowAdd(false); }; return <div className="fade-in"><div className="page-header"><div><div className="page-title">DEAL <span>PIPELINE</span></div><div className="page-sub">{deals.length} aktive Targets · Priorisiert nach Score</div></div><button className="btn btn-primary" onClick={()=>setShowAdd(true)}>+ DEAL HINZUFÜGEN</button></div><div style={{display:'flex',gap:10,marginBottom:20,overflowX:'auto'}}>{stages.map(s=><div key={s} className="card" style={{minWidth:130,flex:1}}><div className="card-title">{s}</div><div className="kpi-value" style={{color:deals.filter(x=>x.status===s).length?'var(--accent)':'var(--muted)'}}>{deals.filter(x=>x.status===s).length}</div></div>)}</div><div className="card"><div className="card-title">Alle Targets — Ranking nach Score</div><table className="table"><thead><tr><th>Unternehmen</th><th>Sektor</th><th>Revenue</th><th>EBITDA</th><th>Marge</th><th>Multiple</th><th>Score</th><th>Priorität</th><th>Status</th></tr></thead><tbody>{[...deals].sort((a,b)=>b.score-a.score).map(x=><tr key={x.id}><td className="bold">{x.name}</td><td style={{color:'var(--muted)'}}>{x.sector}</td><td className="mono">€{x.revenue}M</td><td className="mono" style={{color:'var(--accent)'}}>€{x.ebitda}M</td><td className="mono">{x.margin}%</td><td className="mono">{x.multiple}x</td><td><ScoreBar value={x.score}/></td><td><Pill type={x.priority==='High'?'green':x.priority==='Medium'?'orange':'gray'}>{x.priority}</Pill></td><td><Pill type={x.status==='LOI'?'blue':'orange'}>{x.status}</Pill></td></tr>)}</tbody></table></div>{showAdd&&<Modal title="+ NEUES TARGET" onClose={()=>setShowAdd(false)} onSave={add}>{[['Unternehmensname *','name'],['Sektor','sector'],['Umsatz (€M)','revenue'],['EBITDA (€M)','ebitda'],['EV/EBITDA Multiple','multiple']].map(([l,k])=><Field key={k} label={l} value={d[k]} onChange={v=>setD({...d,[k]:v})}/>)}</Modal>}</div>; }

function LBOCalculator(){ const [p,setP]=useState({revenue:30,ebitdaMargin:20,entryMultiple:8,debtPct:60,revenueGrowth:8,marginExpansion:2,exitMultiple:10,holdYears:5}); const up=(k,v)=>setP(x=>({...x,[k]:+v||0})); const ebitda=p.revenue*p.ebitdaMargin/100, ev=ebitda*p.entryMultiple, debt=ev*p.debtPct/100, equity=ev-debt, exitEbitda=ebitda*Math.pow(1+(p.revenueGrowth+p.marginExpansion)/100,p.holdYears), exitEv=exitEbitda*p.exitMultiple, exitEquity=Math.max(0,exitEv-debt*.5), moic=equity?exitEquity/equity:0, irr=moic?((Math.pow(moic,1/p.holdYears)-1)*100):0; return <div className="fade-in"><div className="page-header"><div><div className="page-title">LBO <span>RECHNER</span></div><div className="page-sub">Leveraged Buyout Modell · IRR & MOIC Simulation</div></div></div><div className="grid-2"><div className="card"><div className="card-title">Deal Parameter</div><div className="form-grid">{[['Revenue (€M)','revenue'],['EBITDA-Marge (%)','ebitdaMargin'],['Entry Multiple','entryMultiple'],['Fremdkapital (%)','debtPct'],['Revenue Wachstum p.a. (%)','revenueGrowth'],['Margenverbesserung p.a. (%)','marginExpansion'],['Exit Multiple','exitMultiple'],['Haltedauer','holdYears']].map(([l,k])=><Field key={k} label={l} type="number" value={p[k]} onChange={v=>up(k,v)}/>)}</div></div><div style={{display:'flex',flexDirection:'column',gap:16}}><MetricCard title="Sources & Uses" rows={[['Enterprise Value',`€${ev.toFixed(1)}M`],['Fremdkapital',`€${debt.toFixed(1)}M (${p.debtPct}%)`],['Eigenkapital',`€${equity.toFixed(1)}M`],['EBITDA Entry',`€${ebitda.toFixed(1)}M`]]}/><MetricCard title="Exit Model" rows={[['EBITDA Exit',`€${exitEbitda.toFixed(1)}M`],['EV Exit',`€${exitEv.toFixed(1)}M`],['Equity Exit',`€${exitEquity.toFixed(1)}M`]]}/><div className="card" style={{textAlign:'center'}}><div className="card-title">Returns</div><div className="kpi-value" style={{color:irr>=25?'var(--accent)':irr>=15?'var(--orange)':'var(--red)'}}>{irr.toFixed(1)}%</div><div className="mono" style={{color:'var(--muted)'}}>GROSS IRR · MOIC {moic.toFixed(2)}x</div></div></div></div><div className="card"><div className="card-title">Sensitivitätsanalyse — IRR</div><table className="table"><thead><tr><th>Growth / Exit Multiple</th>{[7,8,9,10,11,12].map(m=><th key={m}>{m}x</th>)}</tr></thead><tbody>{[5,8,10,12,15].map(g=><tr key={g}><td className="mono">+{g}% p.a.</td>{[7,8,9,10,11,12].map(m=>{ const se=ebitda*Math.pow(1+(g+p.marginExpansion)/100,p.holdYears), si=equity?Math.pow(Math.max(0,se*m-debt*.5)/equity,1/p.holdYears)-1:0; return <td key={m} className="mono" style={{color:si>=.25?'var(--accent)':si>=.15?'var(--orange)':'var(--red)'}}>{(si*100).toFixed(1)}%</td>;})}</tr>)}</tbody></table></div></div>}
function MetricCard({title,rows}){return <div className="card"><div className="card-title">{title}</div>{rows.map(([l,v])=><div key={l} style={{display:'flex',justifyContent:'space-between',padding:'8px 0',borderBottom:'1px solid var(--border)'}}><span style={{color:'var(--muted)'}}>{l}</span><span className="mono">{v}</span></div>)}</div>}

function AIAdvisor({ portfolio, deals }){ const [messages,setMessages]=useState([{role:'ai',text:'Willkommen im KI-Beratungsmodus. Ich analysiere dein Portfolio und liefere institutionelle PE-Empfehlungen.'}]); const [input,setInput]=useState(''); const [loading,setLoading]=useState(false); const bottomRef=useRef(null); const quick=['Welches Portfolio-Unternehmen hat das größte Verbesserungspotenzial?','Erstelle einen Value Creation Plan für mein Portfolio','Analysiere die Top-Deals in meiner Pipeline','Welche Exit-Strategie empfiehlst du für die stärksten Assets?','Wo sind die größten KI-Automatisierungspotenziale?']; const send=async(t)=>{const msg=(t||input).trim(); if(!msg)return; setInput(''); setMessages(m=>[...m,{role:'user',text:msg}]); setLoading(true); const portfolioContext=portfolio.map(p=>`${p.name} (${p.sector}): Revenue €${p.revenue}M, EBITDA €${p.ebitda}M, IRR ${p.irr}%, MOIC ${p.moic}x, Score ${p.score}/100, Stage ${p.stage}`).join('\n'); const dealContext=deals.map(d=>`${d.name} (${d.sector}): Revenue €${d.revenue}M, EBITDA €${d.ebitda}M, Marge ${d.margin}%, Multiple ${d.multiple}x, Score ${d.score}/100, Status ${d.status}`).join('\n'); try{ const r=await fetch('/api/ai-advisor',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({prompt:msg,portfolioContext,dealContext})}); const data=await r.json(); setMessages(m=>[...m,{role:'ai',text:r.ok?data.text:`Fehler: ${data.error||'API-Fehler'}`}]); }catch(e){ setMessages(m=>[...m,{role:'ai',text:'API-Verbindungsfehler. Läuft der Backend-Server?'}]); } setLoading(false);}; useEffect(()=>bottomRef.current?.scrollIntoView({behavior:'smooth'}),[messages,loading]); return <div className="fade-in"><div className="page-header"><div><div className="page-title">KI <span>BERATER</span></div><div className="page-sub">Claude via sicherem Backend-Proxy · Institutionelle PE-Analyse</div></div><span className="badge badge-green">LIVE AI</span></div><div className="grid-65-35"><div className="card"><div className="chat-wrap"><div className="chat-messages">{messages.map((m,i)=><div key={i} className={`msg ${m.role==='ai'?'msg-ai':'msg-user'}`}>{m.role==='ai'&&<div className="mono" style={{fontSize:10,color:'var(--accent)',marginBottom:5}}>PE ADVISOR</div>}{m.text}</div>)}{loading&&<div className="msg msg-ai"><div className="typing"><span/><span/><span/></div></div>}<div ref={bottomRef}/></div><div className="chat-input-row"><input className="input chat-input" placeholder="Frage stellen..." value={input} onChange={e=>setInput(e.target.value)} onKeyDown={e=>e.key==='Enter'&&!loading&&send()} disabled={loading}/><button className="btn btn-primary" onClick={()=>send()} disabled={loading}>SENDEN</button></div></div></div><div style={{display:'flex',flexDirection:'column',gap:12}}><div className="card"><div className="card-title">Quick Analysis</div>{quick.map(q=><div key={q} onClick={()=>!loading&&send(q)} style={{padding:'9px 12px',border:'1px solid var(--border)',borderRadius:3,marginBottom:8,cursor:'pointer',fontSize:12,color:'var(--muted)'}}>→ {q}</div>)}</div><div className="card"><div className="card-title">Portfolio Snapshot</div>{portfolio.slice(0,4).map(p=><div key={p.id} style={{display:'flex',justifyContent:'space-between',padding:'6px 0',borderBottom:'1px solid var(--border)'}}><span>{p.name}</span><span className="mono" style={{color:p.irr>=25?'var(--accent)':'var(--orange)'}}>{p.irr}% IRR</span></div>)}</div></div></div></div>}

function Reporting({portfolio,fund}){ const active=portfolio.filter(p=>p.status==='Active'), exits=portfolio.filter(p=>p.status==='Exit'), total=active.reduce((s,p)=>s+p.equity,0), avgIrr=active.reduce((s,p)=>s+p.irr,0)/(active.length||1), avgMoic=active.reduce((s,p)=>s+p.moic,0)/(active.length||1), top=[...active].sort((a,b)=>b.irr-a.irr)[0], weak=[...active].sort((a,b)=>a.irr-b.irr)[0]; return <div className="fade-in"><div className="page-header"><div><div className="page-title">LP <span>REPORTING</span></div><div className="page-sub">Quarterly Report · Vertraulich</div></div><button className="btn btn-primary btn-sm" onClick={()=>window.print()}>↓ EXPORT PDF</button></div><div className="kpi-grid"><div className="kpi-card green"><div className="kpi-label">Gross IRR</div><div className="kpi-value">{avgIrr.toFixed(1)}%</div></div><div className="kpi-card blue"><div className="kpi-label">Avg. MOIC</div><div className="kpi-value">{avgMoic.toFixed(2)}x</div></div><div className="kpi-card orange"><div className="kpi-label">Investiert</div><div className="kpi-value">€{total}M</div></div><div className="kpi-card red"><div className="kpi-label">Exits</div><div className="kpi-value">{exits.length}</div><div className="kpi-delta">DPI: {fund.dpi}x</div></div></div><div className="grid-2"><div className="card"><div className="card-title">Performance Attribution</div><CompanyTable portfolio={active}/></div><div style={{display:'flex',flexDirection:'column',gap:16}}>{top&&<div className="card"><div className="card-title">Top Performer</div><div className="kpi-value" style={{color:'var(--accent)'}}>{top.name}</div><div className="mono">IRR {top.irr}% · MOIC {top.moic}x</div></div>}{weak&&<div className="card"><div className="card-title">Underperformer</div><div className="kpi-value" style={{color:'var(--orange)'}}>{weak.name}</div><div className="mono">IRR {weak.irr}% · MOIC {weak.moic}x</div></div>}<div className="card"><div className="card-title">Realisierte Exits</div>{exits.map(p=><div key={p.id} style={{display:'flex',justifyContent:'space-between',padding:'6px 0'}}><span>{p.name}</span><Pill type="gray">Sold · {p.moic}x</Pill></div>)}</div></div></div></div>}

const NAV=[{id:'dashboard',label:'Dashboard',icon:'◈',section:'ÜBERSICHT'},{id:'markt',label:'Marktdaten',icon:'◎',section:'ÜBERSICHT'},{id:'portfolio',label:'Portfolio',icon:'▣',section:'PORTFOLIO'},{id:'pipeline',label:'Deal Pipeline',icon:'◉',section:'PORTFOLIO'},{id:'lbo',label:'LBO Rechner',icon:'∑',section:'ANALYSE'},{id:'ai',label:'KI Berater',icon:'◆',section:'ANALYSE'},{id:'reporting',label:'LP Reporting',icon:'▤',section:'REPORTING'}];
export default function App(){ const [page,setPage]=useState('dashboard'); const [fund]=useState(initialFund); const [portfolio,setPortfolio]=useState(initialPortfolio); const [deals,setDeals]=useState(initialDeals); const [finnhubKey,setFinnhubKey]=useState(''); const {quotes}=useFinnhub(finnhubKey); const sections=useMemo(()=>NAV.reduce((a,n)=>{(a[n.section]??=[]).push(n);return a;},{}),[]); const render=()=>({dashboard:<Dashboard fund={fund} portfolio={portfolio} quotes={quotes}/>,markt:<MarktDaten finnhubKey={finnhubKey} setFinnhubKey={setFinnhubKey}/>,portfolio:<Portfolio portfolio={portfolio} setPortfolio={setPortfolio}/>,pipeline:<DealPipeline deals={deals} setDeals={setDeals}/>,lbo:<LBOCalculator/>,ai:<AIAdvisor portfolio={portfolio} deals={deals}/>,reporting:<Reporting portfolio={portfolio} fund={fund}/>}[page]); const bx=quotes.BX; return <><GlobalStyle/><div className="app"><header className="topbar"><div className="topbar-logo">APEX PE <span>Private Equity OS</span></div><span className="badge badge-green">Fund I Active</span><span className="badge badge-blue">€120M AUM</span>{bx&&<span className="mono" style={{color:bx.dp>=0?'var(--accent)':'var(--red)'}}>BX ${bx.c?.toFixed(2)} ({fmtChg(bx.dp)})</span>}<span className="mono" style={{color:'var(--muted)'}}>{new Date().toLocaleDateString('de-DE',{day:'2-digit',month:'short',year:'numeric'})}</span></header><nav className="sidebar">{Object.entries(sections).map(([sec,items])=><div key={sec}><div className="nav-section">{sec}</div>{items.map(n=><div key={n.id} className={`nav-item ${page===n.id?'active':''}`} onClick={()=>setPage(n.id)}><span className="nav-icon">{n.icon}</span>{n.label}</div>)}</div>)}<div style={{flex:1}}/><div style={{padding:'12px 16px',borderTop:'1px solid var(--border)'}}><div className="mono" style={{fontSize:10,color:'var(--muted)',letterSpacing:1}}>FUND MANAGER</div><div style={{fontSize:13,marginTop:4}}>Apex Capital</div>{finnhubKey&&<div className="mono" style={{fontSize:10,color:'var(--accent)',marginTop:4}}>● Finnhub aktiv</div>}</div></nav><main className="main">{render()}</main></div></> }
