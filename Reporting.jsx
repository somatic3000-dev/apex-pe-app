import {Pill} from "../components/UI";
export default function Reporting({portfolio,fund}){const active=portfolio.filter(p=>p.status==="Active"),exits=portfolio.filter(p=>p.status==="Exit"),avgIrr=active.reduce((s,p)=>s+p.irr,0)/active.length,avgMoic=active.reduce((s,p)=>s+p.moic,0)/active.length;return <div><div className="page-header"><div><div className="page-title">LP <span>REPORTING</span></div><div className="page-sub">Quarterly Report</div></div></div><div className="kpi-grid"><div className="kpi-card green"><div className="kpi-label">Gross IRR</div><div className="kpi-value">{avgIrr.toFixed(1)}%</div></div><div className="kpi-card blue"><div className="kpi-label">Avg. MOIC</div><div className="kpi-value">{avgMoic.toFixed(2)}x</div></div><div className="kpi-card orange"><div className="kpi-label">AUM</div><div className="kpi-value">€{fund.aum}M</div></div><div className="kpi-card red"><div className="kpi-label">Exits</div><div className="kpi-value">{exits.length}</div></div></div><div className="card"><table className="table"><thead><tr><th>Unternehmen</th><th>IRR</th><th>MOIC</th><th>Status</th></tr></thead><tbody>{portfolio.map(p=><tr key={p.id}><td>{p.name}</td><td>{p.irr}%</td><td>{p.moic}x</td><td><Pill type={p.status==="Exit"?"gray":"green"}>{p.status}</Pill></td></tr>)}</tbody></table></div></div>}
