import {useState} from "react";import {ScoreBar,Pill,ProgressBar} from "../components/UI";
export default function Portfolio({portfolio,setPortfolio}){const [selected,setSelected]=useState(null);const sel=portfolio.find(p=>p.id===selected);return <div className="fade-in"><div className="page-header"><div><div className="page-title">PORTFOLIO <span>MANAGEMENT</span></div><div className="page-sub">{portfolio.filter(p=>p.status==="Active").length} aktive Beteiligungen</div></div></div><div className="grid-65-35"><div className="card"><table className="table"><thead><tr><th>Unternehmen</th><th>Sektor</th><th>Revenue</th><th>EBITDA</th><th>IRR</th><th>MOIC</th><th>Score</th></tr></thead><tbody>{portfolio.map(p=><tr key={p.id} onClick={()=>setSelected(p.id)}><td className="bold">{p.name}</td><td>{p.sector}</td><td>€{p.revenue}M</td><td className="green">€{p.ebitda}M</td><td>{p.irr}%</td><td className="blue">{p.moic}x</td><td><ScoreBar value={p.score}/></td></tr>)}</tbody></table></div><div className="card">{sel?<><h2>{sel.name}</h2><Pill type="blue">{sel.stage}</Pill><p className="muted">{sel.notes}</p><hr/><ProgressBar label="KI-Potenzial" value={sel.aiScore}/><ProgressBar label="EBITDA Impact" value={Math.max(20,sel.aiScore-25)} color="var(--orange)"/></>:<p className="muted center">← Unternehmen auswählen</p>}</div></div></div>}
